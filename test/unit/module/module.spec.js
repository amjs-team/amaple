import ice from "ice";

describe ( "module =>", () => {
	// let vm;
	// beforeEach ( () => {
	// 	document.body.innerHTML = `<div ice-module="root"><div ice-module="test" :onupdate="resp" ice-src="table"></div></div><a href="#">444</a><div class="abc">{{ title }} {{ content }}</div>`;
	// } );

	// it ( "start the icejs", done => {
	// 	ice.configure ( { baseUrl : "module" } );
	// 	new ice.Module ( "root", {
	// 		init () {
	// 			return {
	// 				resp : function () {
	// 					done ();
	// 				}
	// 			};
	// 		}
	// 	} );
	// } );
} );