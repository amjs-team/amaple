import { argErr } from "./error";
import { type, extend } from "./func/util";

function check ( variable ) {
	let target = variable,
        bools = [];
	
    return {
        or () {

        },
        ifNot ( code, text ) {

        },
        do () {

        },
        toBe ( variable ) {

        },
        toNotBe ( variable ) {

        },
        toType ( string ) {

        },
		toNotType ( string ) {
    		
    	},
	};
}