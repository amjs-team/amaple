import { extend } from "../func/util";

// [
  //  { module: obj1, name: "default", parent: null, children: [
    //    { module: obj2, name: "header", parent: parentObj },
    //    { module: obj3, name: "main", parent: parentObj },
    //    { module: obj3, name: "footer", parent: parentObj }
   // ] }
// ]

export default function Structure () {
	
}

extend ( Structure.prototype, {
	
} );